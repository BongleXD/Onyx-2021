import org.apache.tools.ant.filters.ReplaceTokens

plugins {
    id 'com.github.johnrengelman.shadow' version '5.0.0'
}

dependencies {
    implementation project(':onyx-api')
    compileOnly project(':onyx-bukkit')
    implementation ('com.zaxxer:HikariCP:3.4.5')
    compileOnly files('libs/PotatoSpigot-1.12.2.jar')
    compileOnly files('libs/PotatoSpigot-1.8.8.jar')
}

shadowJar{
    baseName = 'Onyx-Survival'
    classifier = null
    relocate 'com.zaxxer.hikari', 'net.blastmc.onyx.survival.lib.hikari'
}

processResources {
    from(sourceSets.main.resources.srcDirs) {
        filter ReplaceTokens, tokens: [version: version]
    }
}